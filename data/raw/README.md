# Raw Data

The `data/raw/` directory contains unprocessed ingest outputs from services such as `nest_recorder`.
- Nest Recorder writes recordings, logs, and token caches under `data/raw/nest/`.
- Downstream processing writes to `outputs/` (e.g., `outputs/runs/<run_id>/`).
