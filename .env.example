# roll-tracker environment variables
# Copy to .env and adjust paths to match container mounts

# Base directories (defaults shown)
ROLL_TRACKER_CONFIGS_DIR=./configs
ROLL_TRACKER_CAMERA_CONFIGS_DIR=./configs/cameras
ROLL_TRACKER_DATA_DIR=./data
ROLL_TRACKER_OUTPUTS_DIR=./outputs
